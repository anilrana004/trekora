import { Navigate } from "@tanstack/react-router";

export default function DestinationsDistrictPage() {
  // Minimal placeholder — redirects to /destinations until full page is built
  return <Navigate to="/destinations" replace />;
}
