/**
 * @deprecated Prefer `MediaUpload` — kept for backward-compatible imports.
 */
export { default } from "./MediaUpload";
export type { MediaUploadProps as ImageUploadFieldProps } from "./MediaUpload";
